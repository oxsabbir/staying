export const metadata = {
  title: "Airport Taxis",
  description:
    "Book airport taxi transfers with easy scheduling and WhatsApp support on Staying.com.",
};

export default function AirportTaxisLayout({ children }) {
  return children;
}
